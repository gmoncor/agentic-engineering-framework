{
  "_comment": "Configuracion de los hooks SDD. sdd-review-gate BLOQUEA git commit/merge si el diff que se va a commitear no consta revisado; se activa poniendo enabled: true. La revision adversarial por task emite una senal con el hash del diff revisado y el gate la contrasta con git diff --cached. Sin diff cacheado computable degrada a aviso. Solo se cablea en el backend de Claude Code, el unico cuyo flujo emite la senal.",
  "sdd_review_gate": {
    "enabled": false,
    "ttl_hours": 4
  },
  "_comment_read_before_edit": "sdd-read-before-edit AVISA (nunca bloquea) al escribir un archivo existente sin haberlo leido antes en la sesion. enabled controla on/off (default true). mode admite solo 'advisory': cualquier otro valor lo silencia — el hook no bloquea por diseno.",
  "sdd_read_before_edit": {
    "enabled": true,
    "mode": "advisory"
  },
  "_comment_turn_budget": "sdd-turn-budget cuenta las acciones sin commit y avisa al superar cada umbral (warn_at, block_at, hard_stop_at). El contador se resetea al detectar 'git commit'. mode 'advisory' (default) solo avisa; 'enforce' hace que block_at y hard_stop_at DENIEGUEN la accion. hard_stop pide interrumpir y esperar input del usuario. Sin session_id o con enabled: false, se autolimita a silencio.",
  "sdd_turn_budget": {
    "enabled": true,
    "mode": "advisory",
    "warn_at": 30,
    "block_at": 60,
    "hard_stop_at": 90
  },
  "_comment_test_gate": "El gate de tests de /implementar-spec (orquestacion.js) lee sdd_test_gate.command para fijar el comando de test explicito, con prioridad sobre el autodescubrimiento (package.json scripts.test, luego pytest.ini o setup.cfg con [tool:pytest]). Vacio o ausente no es un fallo: cae al autodescubrimiento: sin ningun comando resultante, el gate bloquea solo si la task toca codigo ejecutable; docs/config quedan exentas con aviso.",
  "sdd_test_gate": {
    "command": ""
  },
  "_comment_session_start": "sdd-session-start escribe 1 linea NDJSON de provenance por sesion en ai_docs/audits/provenance.jsonl (append-only). enabled controla on/off (default true): con enabled: false el hook no escribe NADA, porque seguir escribiendo mientras la linea declara la puerta apagada seria un registro que miente sobre su propio origen. Nunca bloquea: sin git, sin directorio de audits, o sin permisos de escritura, degrada a campos null o avisa por stderr y sigue. Destino y campos propios se configuran fuera de este fichero, en el sdd.config.json del proyecto (docs/extension-config-schema.md).",
  "sdd_session_start": {
    "enabled": true
  }
}
