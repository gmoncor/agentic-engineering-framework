{
  "hooks": {
    "BeforeTool": [
      {
        "_comment": "Advisory: avisa si se escribe codigo fuente sin spec aprobada. No bloquea.",
        "matcher": "write_file|edit_file",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${extensionPath}${/}hooks${/}sdd-pipeline-guard.js\""
          }
        ]
      }
    ]
  }
}
